

var shoppingList = [

/* inside goes sting, numbers, boolean values

last element of array doesn't need comma, all other elements should be separated with a comma. */


];


/* To access single element of an array we use an index value. 

var shoppingList = ['dominik', 10, true, 'battery'];

0. 'dominik'
1. 10
2. true
3. 'battery'

*/
